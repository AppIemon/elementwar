/**
 * 드래그 앤 드롭 관련 기능 (제거됨)
 */

// 모든 함수 및 변수 제거 또는 주석 처리
/*
let draggedCard = null;

function initDragAndDrop() {
  // ... 제거됨 ...
}

function handleDragStart(e) {
  // ... 제거됨 ...
}

function handleDragEnd(e) {
  // ... 제거됨 ...
}

function handleDragOver(e) {
  // ... 제거됨 ...
}

function handleDragEnter(e) {
  // ... 제거됨 ...
}

function handleDragLeave(e) {
  // ... 제거됨 ...
}

function handleDrop(e) {
  // ... 제거됨 ...
}

// placeCardOnField 함수는 다른 파일(예: game.js 또는 battlefield.js)로 이동하거나 유지하되,
// 드롭 이벤트 핸들러가 아닌 다른 곳(예: handlePlayerSlotRightClick)에서 호출되도록 수정 필요.
// 현재 battlefield.js에 유사한 로직이 있으므로 여기서는 제거.

// 전역 노출 제거
// window.initDragAndDrop = initDragAndDrop;
// ...
*/

console.log("dragdrop.js: 드래그 앤 드롭 기능이 제거되었습니다. 우클릭 배치로 변경되었습니다.");

// placeCardOnField 함수는 이제 battlefield.js 또는 game.js 등 관련 로직이 있는 곳에서 관리됩니다.
// 이 파일은 비워두거나 삭제해도 됩니다.
